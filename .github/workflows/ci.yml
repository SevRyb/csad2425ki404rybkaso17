name: CI Qt CMake (MSVC 2019) and PlatformIO

on:
  push:
    branches:
      - '*'
  pull_request:
    branches:
      - '*'
  workflow_dispatch:

jobs:
  # Job for building the C++ Qt 5 client project on Windows using MSVC 2019 x64
  client-build-windows:
    runs-on: windows-latest

    steps:
      # Step 1: Checkout the repository
      - name: Checkout repository
        uses: actions/checkout@v4

      # Step 2: Install CMake
      - name: Install CMake
        uses: lukka/get-cmake@v3.21.2

      # Step 3: Install Qt 5 on Windows
      - name: Install Qt 5
        run: |
          choco install qt5-default
          echo "Qt 5 installed"

      # Step 4: Configure project using CMake with MSVC 2019 x64
      - name: Configure with CMake
        run: cmake -G "Visual Studio 16 2019" -A x64 ..
        working-directory: client/build

      # Step 5: Build the project using MSVC
      - name: Build client app
        run: cmake --build . --config Release
        working-directory: client/build

      # Step 6: Upload client build artifacts
      - name: Upload Client build artifact
        uses: actions/upload-artifact@v4
        with:
          name: client-build-windows
          path: client/build/

  # Job for building the PlatformIO server project
  server-build:
    runs-on: ubuntu-latest

    steps:
      # Step 1: Checkout the repository
      - name: Checkout repository
        uses: actions/checkout@v4

      # Step 2: Install PlatformIO
      - name: Install PlatformIO
        run: |
          python3 -m pip install -U platformio

      # Step 3: Build PlatformIO project
      - name: Build PlatformIO project
        run: pio run
        working-directory: server

      # Step 4: Upload PlatformIO build artifacts
      - name: Upload Server build artifact
        uses: actions/upload-artifact@v4
        with:
          name: server-build
          path: server/.pio/build/*
